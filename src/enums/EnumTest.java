package enums;
// used when there's a predetermined group of objects that you need to choose from
// defines names similar to global variables, but without a value. sort of like a "marker"
// one option:
public enum EnumTest{ONE, TWO, THREE}

// another option:

enum EnumTest2{ONE, TWO, THREE}